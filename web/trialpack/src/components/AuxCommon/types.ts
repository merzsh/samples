export enum EAuxSize { S, M, L }
export enum EAuxAlignH { L, C, R }
export enum EAuxAlignV { T, M, B }

// export enum EProjPlannerColIds { A = 'A', B = 'B' }
// Object.values(EProjPlannerColIds)[index];
